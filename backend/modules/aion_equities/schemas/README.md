	•	v0_1 = initial locked schema pack for AION Equity Intelligence runtime
	•	all write events must include schema version refs
	•	breaking changes create a new folder (v0_2), don’t silently overwrite behavior
