#!/usr/bin/env bash
set -euo pipefail

mkdir -p \
  CONNECTIVITY/src/programmable_connectivity \
  CONNECTIVITY/tests/programmable_connectivity \
  CONNECTIVITY/artifacts/programmable_connectivity \
  CONNECTIVITY/docs

echo "OK: scaffold dirs created. Now run the per-file cat blocks."
