From HEAD Mon Sep 17 00:00:00 2001
From: Hypothesis 6.139.2 <no-reply@hypothesis.works>
Date: Tue, 13 Jan 2026 09:54:36
Subject: [PATCH] Hypothesis: add explicit examples

---
--- ./backend/photon_algebra/tests/test_bounded_confluence.py
+++ ./backend/photon_algebra/tests/test_bounded_confluence.py
@@ -94,6 +94,9 @@
 
 @settings(max_examples=250, deadline=None)
 @given(exprs)
+@example(
+    e={'op': '⊗', 'states': ['a', 'b', {'op': '⊕', 'states': ['a', 'b']}]},
+).via('discovered failure')
 def test_bounded_confluence_via_one_step_joinability(e):
     """
     “Bounded confluence” evidence: for small core expressions, a handful of
--- ./backend/photon_algebra/tests/test_properties_normalize.py
+++ ./backend/photon_algebra/tests/test_properties_normalize.py
@@ -94,6 +94,10 @@
 
 @settings(max_examples=200, deadline=None)
 @given(exprs, exprs)
+@example(
+    a={'op': '⊕', 'states': ['a', 'c']},
+    b={'op': '⊗', 'states': ['c', {'op': '⊕', 'states': ['a', 'b']}]},
+).via('discovered failure')
 def test_commutativity_stability_product(a, b):
     assert rewriter.normalize(deepclone(PROD(a, b))) == rewriter.normalize(deepclone(PROD(b, a)))
 
@@ -108,6 +112,11 @@
 
 @settings(max_examples=200, deadline=None)
 @given(exprs, exprs, exprs)
+@example(
+    a='a',
+    b='b',
+    c={'op': '⊕', 'states': ['a', 'b']},
+).via('discovered failure')
 def test_associativity_stability_product(a, b, c):
     left = PROD(PROD(a, b), c)
     right = PROD(a, PROD(b, c))
@@ -116,6 +125,10 @@
 
 @settings(max_examples=200, deadline=None)
 @given(atoms, exprs)
+@example(
+    a='a',
+    b={'op': '⊗', 'states': [{'op': '⊕', 'states': ['a', 'b']}, 'c']},
+).via('discovered failure')
 def test_absorption_sanity(a, b):
     # Use atomic 'a' to avoid annihilator/identity corner cases
     lhs = SUM(a, PROD(a, b))
