# pytest.ini
[pytest]
# ──────────────────────────────────────────────
# ✅ Test discovery configuration
# ──────────────────────────────────────────────
# Only collect tests inside the Symatics module
testpaths =
    backend/symatics/tests

# Add the repository root to PYTHONPATH
# so imports like `from backend.symatics import ...` resolve correctly
pythonpath =
    .

# ──────────────────────────────────────────────
# 🚫 Ignore irrelevant directories
# ──────────────────────────────────────────────
norecursedirs =
    .git
    .venv
    venv
    node_modules
    dist
    build
    google-cloud-sdk
    frontend
    scripts
    data
    docs
    notebooks
    tmp
    __pycache__

# ──────────────────────────────────────────────
# 🧹 Output & warnings
# ──────────────────────────────────────────────
addopts = -v -ra --tb=short

filterwarnings =
    # Show deprecations once per location
    default::DeprecationWarning
    default::PendingDeprecationWarning

    # Suppress irrelevant user warnings
    ignore::UserWarning
    ignore::RuntimeWarning

# ──────────────────────────────────────────────
# 🏷️ Markers (custom test groups)
# ──────────────────────────────────────────────
markers =
    legacy: legacy tests kept for reference
    glyphchain: GlyphChain integration/regression gates