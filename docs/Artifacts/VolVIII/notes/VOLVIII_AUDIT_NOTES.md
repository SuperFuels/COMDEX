# Volume VIII — Audit Notes (v0.1)

Purpose: define a reviewer-proof *audit surface* for a self-referential loop:
Psi ↔ mu(recurse Psi)

Lock criteria:
- Deterministic byte-stable trace after canonical normalization
- Metrics snapshot meets thresholds
- Event schema conforms to Vol V contract keys/types

Next tighten:
- Add explicit K_sem metric + bound
- Add pi_s recording (either reuse Vol VII or re-estimate)
