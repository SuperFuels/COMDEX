# Metrics Definitions (as implemented)

- Selectivity S := target / mean(others)
- Crosstalk X := max(others) / target
- Convergence time tSync := first time R >= threshold
- Advantage (A4) adv := gap_no / max(gap_yes, eps) or per test implementation
- ΔCoupling (A3.1) := mean(match) - mean(mismatch) and separation via σ (per test)

Lock ID: P12-SIM-2026-01-02-A2A4A31
Status: LOCKED
Maintainer: Tessaris AI
Author: Kevin Robinson.
