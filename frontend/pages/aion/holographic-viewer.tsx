import HolographicViewer from "@/components/Hologram/HolographicViewer";

export default function HolographicViewerPage() {
  // you can pass props if needed (like containerId)
  return <HolographicViewer containerId="hqce_main_runtime" />;
}